import { Box } from "@mui/material";
import { styled } from "@mui/system";

const CenterTag = styled(Box)({
  display: "flex",
  justifyContent: "center",
  alignItems: "center",
});

export default CenterTag;
